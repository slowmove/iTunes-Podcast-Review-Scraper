{% include 'header.twig' %}
<h1>Podcasts</h1>
<div class="row">
	{% for podcast in podcasts %}
		<div class="col-md-6">
			{{ podcast.label }}
		</div>
		<div class="col-md-6">
			<a href="/podcast/{{ podcast.podcastId }}/reviews">
				Reviews
			</a>
		</div>	
	{% endfor %}
</div>
{% include 'footer.twig' %}